---
---

<section id="inicio" class="relative min-h-screen flex items-center justify-center overflow-hidden">
  <div class="absolute inset-0 bg-gradient-to-br from-blue-600 via-blue-700 to-green-600"></div>
  
  <!-- Animated background elements -->
  <div class="absolute inset-0 opacity-10">
    <div class="floating-element absolute top-20 left-20 w-32 h-32 bg-white/20 rounded-full"></div>
    <div class="floating-element absolute top-40 right-32 w-24 h-24 bg-white/15 rounded-full" style="animation-delay: 1s;"></div>
    <div class="floating-element absolute bottom-32 left-40 w-20 h-20 bg-white/10 rounded-full" style="animation-delay: 2s;"></div>
  </div>
  
  <div class="relative z-10 text-center text-white px-4 max-w-4xl mx-auto">
    <div class="hero-content">
      <h1 class="text-5xl md:text-7xl font-bold mb-6 leading-tight">
        Bienvenidos a nuestro
        <span class="text-transparent bg-clip-text bg-gradient-to-r from-green-300 to-blue-300">
          cuidado responsable
        </span>
      </h1>
      
      <p class="text-xl md:text-2xl mb-8 opacity-90 max-w-2xl mx-auto">
        Brindamos soluciones de cuidado responsable para gestión ambiental, 
        aprovechando la energía renovable y un impacto positivo en nuestro planeta.
      </p>
      
      <div class="flex flex-col sm:flex-row gap-4 justify-center">
        <button class="btn-primary px-8 py-4 rounded-full text-lg font-semibold bg-green-500 hover:bg-green-400 transition-all duration-300 transform hover:scale-105">
          Nuestros Servicios
        </button>
        <button class="btn-secondary px-8 py-4 rounded-full text-lg font-semibold border-2 border-white hover:bg-white hover:text-gray-800 transition-all duration-300">
          Instituciones
        </button>
      </div>
    </div>
    
    <!-- Plant growing animation -->
    <div class="plant-container absolute bottom-10 left-1/2 transform -translate-x-1/2">
      <div class="plant-stem"></div>
      <div class="plant-leaves">
        <div class="leaf leaf-1"></div>
        <div class="leaf leaf-2"></div>
      </div>
    </div>
  </div>
</section>

<style>
  .hero-content {
    animation: fadeInUp 1s ease-out;
  }

  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(40px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .floating-element {
    animation: float 6s ease-in-out infinite;
  }

  @keyframes float {
    0%, 100% { transform: translateY(0px); }
    50% { transform: translateY(-20px); }
  }

  .btn-primary::after {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: linear-gradient(45deg, transparent, rgba(255,255,255,0.1), transparent);
    transform: translateX(-100%);
    transition: transform 0.6s;
  }

  .btn-primary:hover::after {
    transform: translateX(100%);
  }

  .btn-primary {
    position: relative;
    overflow: hidden;
  }

  /* Plant animation */
  .plant-container {
    animation: plantGrow 2s ease-out 0.5s both;
  }

  .plant-stem {
    width: 3px;
    height: 40px;
    background: #22c55e;
    margin: 0 auto;
    animation: growStem 1.5s ease-out 1s both;
  }

  .plant-leaves {
    position: relative;
    margin-top: -10px;
  }

  .leaf {
    position: absolute;
    width: 15px;
    height: 8px;
    background: #22c55e;
    border-radius: 0 100% 0 100%;
    animation: leafGrow 0.8s ease-out both;
  }

  .leaf-1 {
    left: -8px;
    top: -5px;
    transform: rotate(-45deg);
    animation-delay: 2s;
  }

  .leaf-2 {
    right: -8px;
    top: -5px;
    transform: rotate(45deg) scaleX(-1);
    animation-delay: 2.3s;
  }

  @keyframes plantGrow {
    from {
      opacity: 0;
      transform: translateX(-50%) scale(0);
    }
    to {
      opacity: 1;
      transform: translateX(-50%) scale(1);
    }
  }

  @keyframes growStem {
    from {
      height: 0;
    }
    to {
      height: 40px;
    }
  }

  @keyframes leafGrow {
    from {
      opacity: 0;
      transform: scale(0) rotate(var(--rotation, 0deg));
    }
    to {
      opacity: 1;
      transform: scale(1) rotate(var(--rotation, 0deg));
    }
  }
</style>