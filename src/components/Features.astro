---
---

<section id="nosotros" class="py-20 bg-gradient-to-br from-green-50 to-blue-50">
  <div class="container mx-auto px-4">
    <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 items-center">
      <!-- Left side - Content -->
      <div class="space-y-8">
        <div class="feature-text">
          <h2 class="text-4xl font-bold text-gray-800 mb-6">
            Hecho para tu equipo de datos
          </h2>
          <p class="text-xl text-gray-600 mb-8">
            En Nova SQL, lo hacemos súper fácil para equipos de datos de todos los tamaños para colaborar en consultas SQL, crear visualizaciones y compartir conocimientos.
          </p>
          
          <div class="space-y-6">
            <div class="feature-item flex items-start space-x-4">
              <div class="feature-icon w-8 h-8 bg-green-500 rounded-full flex items-center justify-center flex-shrink-0">
                <svg class="w-4 h-4 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
                </svg>
              </div>
              <div>
                <h3 class="text-lg font-semibold text-gray-800">Colaboración en Tiempo Real</h3>
                <p class="text-gray-600">Trabaja en equipo con herramientas de colaboración avanzadas</p>
              </div>
            </div>
            
            <div class="feature-item flex items-start space-x-4">
              <div class="feature-icon w-8 h-8 bg-blue-500 rounded-full flex items-center justify-center flex-shrink-0">
                <svg class="w-4 h-4 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z"></path>
                </svg>
              </div>
              <div>
                <h3 class="text-lg font-semibold text-gray-800">Visualizaciones Dinámicas</h3>
                <p class="text-gray-600">Crea gráficos y dashboards interactivos fácilmente</p>
              </div>
            </div>
            
            <div class="feature-item flex items-start space-x-4">
              <div class="feature-icon w-8 h-8 bg-purple-500 rounded-full flex items-center justify-center flex-shrink-0">
                <svg class="w-4 h-4 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z"></path>
                </svg>
              </div>
              <div>
                <h3 class="text-lg font-semibold text-gray-800">Seguridad Enterprise</h3>
                <p class="text-gray-600">Protección de datos con estándares empresariales</p>
              </div>
            </div>
          </div>
        </div>
      </div>
      
      <!-- Right side - Mock interfaces -->
      <div class="relative">
        <div class="mockup-container relative">
          <!-- Python Notebook mockup -->
          <div class="mockup-card bg-gray-900 rounded-lg p-4 shadow-2xl transform rotate-3 hover:rotate-0 transition-transform duration-300">
            <div class="flex items-center space-x-2 mb-4">
              <div class="w-3 h-3 bg-red-500 rounded-full"></div>
              <div class="w-3 h-3 bg-yellow-500 rounded-full"></div>
              <div class="w-3 h-3 bg-green-500 rounded-full"></div>
              <span class="text-gray-400 text-sm ml-2">Python Notebook</span>
            </div>
            <div class="space-y-2">
              <div class="h-4 bg-green-400 rounded w-3/4"></div>
              <div class="h-4 bg-blue-400 rounded w-1/2"></div>
              <div class="h-4 bg-purple-400 rounded w-2/3"></div>
            </div>
          </div>
          
          <!-- Dashboard mockup -->
          <div class="mockup-card bg-white rounded-lg p-4 shadow-2xl absolute top-8 right-4 transform -rotate-2 hover:rotate-0 transition-transform duration-300">
            <div class="flex items-center justify-between mb-4">
              <h4 class="text-sm font-semibold text-gray-800">Analytics Dashboard</h4>
              <div class="flex space-x-1">
                <div class="w-2 h-2 bg-green-500 rounded-full"></div>
                <div class="w-2 h-2 bg-blue-500 rounded-full"></div>
                <div class="w-2 h-2 bg-red-500 rounded-full"></div>
              </div>
            </div>
            <div class="space-y-2">
              <div class="h-12 bg-gradient-to-r from-green-200 to-blue-200 rounded"></div>
              <div class="grid grid-cols-2 gap-2">
                <div class="h-8 bg-gray-100 rounded"></div>
                <div class="h-8 bg-gray-100 rounded"></div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .feature-text {
    animation: fadeInLeft 0.8s ease-out;
  }

  .feature-item {
    opacity: 0;
    animation: slideInRight 0.6s ease-out forwards;
  }

  .feature-item:nth-child(1) {
    animation-delay: 0.2s;
  }

  .feature-item:nth-child(2) {
    animation-delay: 0.4s;
  }

  .feature-item:nth-child(3) {
    animation-delay: 0.6s;
  }

  @keyframes fadeInLeft {
    from {
      opacity: 0;
      transform: translateX(-30px);
    }
    to {
      opacity: 1;
      transform: translateX(0);
    }
  }

  @keyframes slideInRight {
    from {
      opacity: 0;
      transform: translateX(30px);
    }
    to {
      opacity: 1;
      transform: translateX(0);
    }
  }

  .mockup-container {
    animation: float 6s ease-in-out infinite;
  }

  .mockup-card::after {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(45deg, transparent, rgba(255,255,255,0.1), transparent);
    transform: translateX(-100%);
    transition: transform 0.6s;
  }

  .mockup-card:hover::after {
    transform: translateX(100%);
  }

  .mockup-card {
    position: relative;
    overflow: hidden;
  }

  .feature-icon::before {
    content: '';
    position: absolute;
    inset: -2px;
    background: linear-gradient(45deg, currentColor, transparent, currentColor);
    border-radius: 50%;
    z-index: -1;
    animation: pulse 2s infinite;
  }

  .feature-icon {
    position: relative;
  }

  @keyframes pulse {
    0%, 100% {
      transform: scale(1);
      opacity: 1;
    }
    50% {
      transform: scale(1.1);
      opacity: 0.7;
    }
  }
</style>